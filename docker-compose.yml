version: '3.8'

services:
  postgres_nlw:
    container_name: postgres_nlw
    image: postgres
    ports:
      - 5443:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=PapaBravo135@
      - POSTGRES_DB=pg_nlw